//This is headers of function, which is used for work with buffer
//Headers are needed to compile files, which uses this functions (for find_token )

int
buf_inizialization(char **buf);

int
buf_realloc(char **buf, int index);
